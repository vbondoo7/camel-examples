<?xml version="1.0"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:sec="http://www.springframework.org/schema/security"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="    http://www.springframework.org/schema/beans      http://www.springframework.org/schema/beans/spring-beans.xsd                   http://camel.apache.org/schema/spring     http://camel.apache.org/schema/spring/camel-spring.xsd           http://www.springframework.org/schema/security   http://www.springframework.org/schema/security/spring-security-4.1.xsd           http://www.springframework.org/schema/context    http://www.springframework.org/schema/context/spring-context-4.3.xsd">

	<context:component-scan base-package="com.od.eai.framework.core" />
	<bean class="com.od.eai.framework.core.interceptor.AsyncContainerWideInterceptorImpl" id="cwInterceptor" />

	<!-- THREAD POOL FOR INTERECEPTOR -->
	<bean id="interceptorPool"
		class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
		<property name="corePoolSize" value="${eai.framework.core.interceptor.threadpool.corePoolSize:20}" />
		<property name="maxPoolSize" value="${eai.framework.core.interceptor.threadpool.maxPoolSize:50}" />
		<property name="queueCapacity" value="${eai.framework.core.interceptor.threadpool.queueCapacity:1000}" />
		<property name="WaitForTasksToCompleteOnShutdown" value="${eai.framework.core.interceptor.threadpool.gracefullShutdown:true}" />
	</bean>
	
	<!-- -->
	<!-- LOWEST LEVEL CONNECTION FACTORY -->
	<!-- -->
	<bean id="enaMessagingConnectionFactory" class="org.apache.qpid.jms.JmsConnectionFactory" primary="true">
		<property name="remoteURI" value="${errorandaudit.messaging.provider.url}" />
		<property name="username" ref="ERRORANDAUDIT_MESSAGING_PROVIDER_USERNAME" />
		<property name="password" ref="ERRORANDAUDIT_MESSAGING_PROVIDER_PASSWORD" />
	</bean>

	<!-- -->
	<!-- POOLED CONNECTION FACTORY (as we're using this only for producing)-->
	<!-- -->
	<bean id="pooledenaMessagingConnectionFactory" class="org.apache.activemq.jms.pool.PooledConnectionFactory">
		<property name="connectionFactory" ref="enaMessagingConnectionFactory" />
		<property name="maxConnections" value="${errorandaudit.messaging.provider.maxProducerConnections:3}" />
		<property name="maximumActiveSessionPerConnection" value="${errorandaudit.messaging.provider.maxActiveSessionsPerConnection:500}" />
	</bean>

	<bean id="errorandaudit-out" class="org.apache.camel.component.jms.JmsComponent">
      <property name="connectionFactory" ref="pooledenaMessagingConnectionFactory" />
   </bean>

	
	<!-- Obfuscator config import as framework depends on it -->
	<import resource="classpath:META-INF/spring/obf-config.xml" />

</beans>
